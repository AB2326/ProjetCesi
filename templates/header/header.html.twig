<head>
    <header>
        <div class="container-fluid navbar">
            <a class="navbar-brand" href="/">
                <span class="navbar-brand-text">(RE)SOURCES RELATIONNELLES!</span>
            </a>
            <div class="accessibility-button">
                <span>Accessibilité</span>
                <a href=""><i class="fas fa-lightbulb"></i></a>
            </div>
        </div>
        <nav class="navbar navbar-expand-lg navbar-light" style="background-color: #0055a4;">
            <div class="container-fluid">
                <form class="d-flex">
                    <input class="form-control me-2" type="search" placeholder="Rechercher" aria-label="Search">
                    <button class="btn btn-outline-light" type="submit">Search</button>
                </form>
                <div class="d-flex">
                    <a href="{{ path('app_login') }}" class="btn btn-outline-light me-2">Connexion</a>
                    <a href="{{ path('app_register') }}" class="btn btn-outline-light me-2">Inscription</a>
                </div>
            </div>
        </nav>
    </header>
</head>

<style>
    header {
        background-color: #0055a4; /* Couleur bleue pour la navbar */
        color: white;
        padding: 10px 20px; /* Espacement autour des éléments de la navbar */
    }

    .navbar-brand {
        color: white; /* Logo ou texte en blanc */
        text-decoration: none; /* Pas de soulignement */
    }

    .navbar-brand-text {
        font-weight: bold; /* Texte en gras */
    }

    .accessibility-button a, .navbar a.btn {
        color: white; /* Liens en blanc */
        background: transparent;
        border: 2px solid white; /* Bordure blanche pour les boutons */
        padding: 5px 10px; /* Padding pour les boutons */
        border-radius: 5px; /* Bordures arrondies */
        text-decoration: none; /* Pas de soulignement */
    }

    .accessibility-button i {
        margin-left: 5px; /* Espacement à gauche de l'icône */
    }
</style>
